import java.io.IOException;
import org.junit.jupiter.api.Test;

public class SearchTest {

	@Test
	public void testSearch() throws IOException {
//		ArrayList<Media> movieList = MovieJson.searchBasic("inception");
//		ArrayList<Media> movies = MovieJson.searchMovie("the office");
//    ArrayList<Media> series = MovieJson.searchSeries("the office");
		
//		assertEquals("Inception", movieList.get(0).getTitle());
//		assertEquals("(2020)", movies.get(0).getDescription());
	}
	
	@Test
	public void testSearchMovie() {
//		ArrayList<Media> avatar = MovieJson.searchMovie("Avatar");
		
//		assertEquals("Avatar: The Way of Water", avatar.get(0).getTitle());
//		assertEquals("(2022)", avatar.get(0).getDescription());
	}
}
